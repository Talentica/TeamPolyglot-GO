1. Our program began with package main. What would the files in the fmt package begin with?

2. Given the array:

	x := [6]string{"a","b","c","d","e","f"}
	what would x[2:5] give you?

3. Write a function with one variadic parameter that finds the greatest number in a list of numbers.

4. What are defer, panic and recover? How do you recover from a run-time panic?

5. Add a new method to the Shape interface called perimeter which calculates the perimeter of a shape. Implement the method for Circle and Rectangle.